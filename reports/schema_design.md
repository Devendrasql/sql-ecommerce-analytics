The database schema for ShopHub is designed following Third Normal Form (3NF).

Each table represents a single business entity such as customers,
orders, products, sellers, and payments.

Primary keys uniquely identify each record and foreign keys enforce
relationships between tables.

This structure eliminates data redundancy and ensures that every
non-key attribute depends only on the primary key of its table.